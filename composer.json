{
	"name"        : "perf/type-validation",
	"type"        : "library",
	"license"     : "MIT",
	"description" : "Allows to check if a variable is of the expected type, using a type specification string like the ones in PHPDoc blocks (int, \\Foo\\Bar[], null|string, etc).",
	"require"     : {
		"php"          : ">=7.4",
		"perf/caching" : "^3.0"
	},
	"require-dev": {
		"ext-xdebug"                : "*",
		"phing/phing"               : "^2.0",
		"phpmd/phpmd"               : "^2.0",
		"phpunit/phpunit"           : "^9.2",
		"squizlabs/php_codesniffer" : "^3.5"
	},
	"autoload" : {
		"psr-4" : {
			"perf\\TypeValidation\\" : "src/"
		}
	}
}
